# AppImage for qhuaweiflash 
This repository provides a qhuaweiflash AppImage build and Makefile to build it on the Debian oldstable (jessie) LXC container.

## How to build? 
Fresh Debian Jessie + 3 commands: 
```
sudo apt-get install make
sudo make dep
make
```
And you get AppImage!
